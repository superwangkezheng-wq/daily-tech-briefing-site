PORT=4321
HOST=0.0.0.0
SITE_TITLE=每日科技信息
FIXED_SITE_URL=http://localhost:4321
NEWS_ARCHIVE_DIR=./data/collections
FEEDBACK_DIR=./data/feedback
FEEDBACK_DIGEST_DIR=./data/feedback/_digest
MAINTENANCE_DIR=./data/maintenance
CACHE_DIR=./.cache
WEEKLY_INSIGHT_SOURCE_DIR=./data/weekly-insights
WEEKLY_INSIGHT_CACHE_DIR=./.cache/weekly-insights
WEEKLY_INSIGHT_FEEDBACK_DIR=./data/weekly-feedback
# Required to view public_enabled=false snapshots. Leave production unset until internal preview is configured.
WEEKLY_PREVIEW_TOKEN=
# Independent private credential for final Word feedback and WBR acknowledgements. Never put this token in a URL.
WEEKLY_FEEDBACK_TOKEN=
WEEKLY_FEEDBACK_MAX_DOCX_BYTES=8388608
# Required only by `npm run mcp:http`; use a unique local secret in the Authorization: Bearer header.
MCP_HTTP_TOKEN=
DAILY_COLLECTION_SLOTS=morning
MORNING_COLLECTION_TIME=09:40
MORNING_REFRESH_LAG_MINUTES=20
MORNING_REFRESH_MAX_ATTEMPTS=36
MORNING_REFRESH_RETRY_DELAY_MINUTES=10
AFTERNOON_COLLECTION_TIME=15:00
AFTERNOON_REFRESH_LAG_MINUTES=20
AFTERNOON_REFRESH_MAX_ATTEMPTS=6
AFTERNOON_REFRESH_RETRY_DELAY_MINUTES=5
EVENING_COLLECTION_TIME=20:00
EVENING_REFRESH_LAG_MINUTES=20
EVENING_REFRESH_MAX_ATTEMPTS=6
EVENING_REFRESH_RETRY_DELAY_MINUTES=5
SUMMARIZE_WRAPPER=
OPENCLAW_BIN=openclaw
FEISHU_ACCOUNT=default
FEISHU_TARGET=
MAINTENANCE_TOKEN=replace-with-a-local-maintenance-token
WIKI_SOURCE_DIR=
QMD_BIN=qmd
QMD_COLLECTION_NAME=daily-tech-site-wiki
QMD_CONTEXT_TEXT=

# --- Horizon-inspired features ---
# AI score threshold (0=show all, backwards compatible, 6.0=recommended when scoring is stable)
AI_SCORE_THRESHOLD=0
# Per-section category limits (0=unlimited)
CATEGORY_GROUP_TECHNEWS=0
CATEGORY_GROUP_VIDEO=3
CATEGORY_GROUP_CREATOR=5
# Enrichment worker (true/false)
ENRICH_ENABLED=false
# Enrichment data directory
ENRICH_DIR=./data/enrich
# --- End Horizon-inspired features ---
